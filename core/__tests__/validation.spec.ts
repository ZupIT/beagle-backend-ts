import { componentValidation } from 'src'

describe('Component validation', () => {
  it('should validate single component (success)', () => {

  })

  it('should validate single component (error)', () => {

  })

  it('should validate component tree (success)', () => {

  })

  it('should validate component tree (error)', () => {

  })
})
