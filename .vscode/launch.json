{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RUN AND DEBUG",
      "type": "node",
      "program": "${workspaceFolder}/sample/src/index.ts",
      "request": "launch",
      "args": [
        "${relativeFile}"
      ],
      "runtimeArgs": [
        "-r",
        "ts-node/register"
      ],
      "cwd": "${workspaceRoot}",
      "protocol": "inspector",
    }
  ]
}
